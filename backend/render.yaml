{
  "name": "ai-painter-backend",
  "version": "2.0.0",
  "buildCommand": "npm install",
  "startCommand": "npm start",
  "envVars": {
    "DEEPSEEK_API_KEY": {
      "description": "DeepSeek API Key"
    },
    "NODE_ENV": {
      "value": "production"
    }
  }
}
